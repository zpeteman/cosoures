\problem{
Let
\[
F(x)=\int_0^1 \frac{dt}{(t^2+1)(t^2+x^2)}.
\]

\begin{enumerate}
\item Prove that $F$ is continuous on $(0,+\infty)$.
\item Deduce the value of
\[
\int_0^1 \frac{dt}{(t^2+1)^2}.
\]
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Let $x>0$, $b\neq 0$, and define
\[
F(x)=\int_0^{+\infty} e^{-tx}\frac{\sin(bt)}{t}\,dt.
\]

\begin{enumerate}
\item Prove that $F$ is differentiable on $(0,+\infty)$ and compute $F'(x)$.
\item Compute $\lim_{x\to+\infty}F(x)$ and $\lim_{x\to 0^+}F(x)$.
\item Deduce the value of
\[
\int_0^{+\infty}\frac{\sin(bt)}{t}\,dt.
\]
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Let $x>0$, $b\neq 0$, and define
\[
F(x)=\int_0^{+\infty} e^{-tx}\frac{\sin(bt)}{t}\,dt.
\]

\begin{enumerate}
\item Prove that $F$ is differentiable on $(0,+\infty)$ and compute $F'(x)$.
\item Compute $\lim_{x\to+\infty}F(x)$ and $\lim_{x\to 0^+}F(x)$.
\item Deduce the value of
\[
\int_0^{+\infty}\frac{\sin(bt)}{t}\,dt.
\]
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
For every $x\in\mathbb{R}$, define
\[
F(x)=\int_0^{\pi/2}\ln\!\left(\cos^2(t)+x^2\sin^2(t)\right)dt.
\]

\begin{enumerate}
\item Prove that $F$ is continuous on $\mathbb{R}$ and differentiable on $\mathbb{R}^*$.
\item Compute $F'(x)$ and then determine $F(x)$.
\item Deduce the value of
\[
\int_0^{\pi/2}\ln(\cos t)\,dt.
\]
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Define $f$ and $g$ on $\mathbb{R}^+$ by
\[
f(x)=\int_0^{+\infty}\frac{e^{-xt}}{1+t^2}dt,
\qquad
g(x)=\int_0^{+\infty}\frac{\sin t}{t+x}dt.
\]

\begin{enumerate}
\item Prove that $f$ and $g$ are of class $C^2$ on $(0,+\infty)$ and satisfy the differential equation
\[
y''+y=\frac{1}{x}.
\]
\item Prove that $f$ and $g$ are continuous at $0$.
\item Prove that $\lim_{x\to+\infty}f(x)=\lim_{x\to+\infty}g(x)=0$.
\item Deduce that $g(x)=f(x)$ and compute
\[
\int_0^{+\infty}\frac{\sin t}{t}dt.
\]
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Let $x,y>0$ and define
\[
F(x,y)=\int_0^{+\infty}\frac{e^{-xt}-e^{-yt}}{t}dt.
\]

\begin{enumerate}
\item For fixed $y>0$, prove that $x\mapsto F(x,y)$ is $C^1$ on $(0,+\infty)$.
\item Compute $\frac{\partial F}{\partial x}(x,y)$.
\item Deduce the value of $F(x,y)$.
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Let $a,b>0$ and define
\[
F(x)=\int_0^{+\infty}\frac{e^{-at}-e^{-bt}}{t}\cos(xt)\,dt.
\]

\begin{enumerate}
\item Prove that the domain of $F$ is $\mathbb{R}$.
\item Prove that $F$ is of class $C^1$ on $\mathbb{R}$ and compute $F'(x)$.
\item Prove that $\lim_{x\to+\infty}F(x)=0$ and deduce the explicit expression of $F(x)$.
\end{enumerate}
}

\begin{solution}

\end{solution}

\problem{
Consider the function $T:\mathbb{R}\to\mathbb{R}$ defined by
\[
T(x)=\int_0^{+\infty} t^{x-1}e^{-t}\,dt.
\]

\begin{enumerate}
\item Determine the domain of definition $D_T$ of $T$ (Euler's Gamma function).
\item Prove that $T(x+1)=xT(x)$ for all $x\in D_T$. Deduce that $T(n+1)=n!$ and $T(x)=\Gamma(x)$.
\item Prove that $T$ is convex and determine its variation.
\item Compute $T\!\left(\frac12\right)$ and deduce $\Gamma\!\left(n+\frac12\right)$ for all $n\in\mathbb{N}$.
\item Let
\[
I_n=2\int_0^{+\infty} u^n e^{-u^2}\,du.
\]
Compute $I_{2n}$ and $I_{2n+1}$.
\end{enumerate}
}

\begin{solution}

\end{solution}